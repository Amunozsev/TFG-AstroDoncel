# AstroDoncel backend dependencies
# Used by Railway to install the FastAPI backend.
# Vercel does not use this file; Vercel uses frontend/package.json.

# Web API
fastapi==0.139.0
uvicorn[standard]==0.49.0
pydantic==2.13.4

# Scientific processing
astropy==8.0.0
numpy==2.5.0
scipy==1.18.0

# GOES/XRS overlay through SunPy/Fido
sunpy[net,timeseries]==8.0.0
mpl-animators==1.2.4
pandas==3.0.3

# Optional ML burst detection:
# The /api/burst/detect endpoint degrades gracefully if torch is missing.
# Install torch only when the Railway plan has enough RAM/build time:
# torch --index-url https://download.pytorch.org/whl/cpu
