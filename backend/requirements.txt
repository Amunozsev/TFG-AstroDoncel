# AstroDoncel – Backend  (Python 3.12+)
# Instalar con:  pip install -r requirements.txt

# --- Framework web ---
fastapi==0.115.12
uvicorn[standard]==0.34.2      # servidor ASGI con recarga automática

# --- Procesamiento científico / astronómico ---
astropy==7.0.1
numpy==2.2.5
scipy==1.15.3

# --- Validación de datos (incluida en FastAPI, se especifica por claridad) ---
pydantic==2.11.4

# --- Futuras dependencias (se activarán en sprints posteriores) ---
# sqlalchemy==2.0.40           # ORM para PostgreSQL
# asyncpg==0.30.0              # driver async para PostgreSQL
# psycopg2-binary==2.9.10      # driver síncrono (migrations con Alembic)
# tensorflow==2.19.0           # modelo CNN de clasificación de SRBs
# scikit-learn==1.6.1          # preprocesamiento para el modelo de IA
