-r requirements-dev.txt

# Optional model export/retraining tools. The API uses onnxruntime from
# requirements.txt and does not import PyTorch while serving requests.
onnx==1.18.0
psutil==7.0.0
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.7.0+cpu
