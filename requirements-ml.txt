-r requirements.txt

# Optional ONNX export toolchain. Install requirements-dev.txt separately when
# contributing. The served API uses onnxruntime and never imports PyTorch.
onnx==1.18.0
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.7.0+cpu
